// import 'server-only';
// - this is an npm package thats throws an error if you imported this component by accedant on a client component
const Test = () => {
	return (
		<>
			<div className=''>Test</div>
		</>
	);
};

export default Test;
